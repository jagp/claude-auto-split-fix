# Build tooling only (vsce); the extension itself has no runtime dependencies.
node_modules/
src/node_modules/**

# VS Code test build downloaded by npm run test:vscode.
.vscode-test/

# Per-project session log written by the omnilog tooling.
omnilog.md

# Trunk ignores its own caches via .trunk/.gitignore.
